# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Segredos e dados pessoais reais de alunos (scripts avulsos de correção
# pontual, planilhas extraídas, dumps de docs de referência) — nunca
# versionar. Ver auditoria de segurança de 2026-09-05: hoje nenhum desses
# arquivos está no histórico do Git, mas sem essas regras um `git add -A`
# desavisado os comitaria (PII real: CPF, RG, endereço, telefone).
.env
.env.*
!.env.example
fix_*.sql
update_dados_*.sql
audit_verificacao_*.sql
planilha_*_extraida.json
docs_extracted/
